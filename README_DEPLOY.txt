*** Instrucciones para desplegar en Firebase Hosting ***
1. Entrar a la carpeta del proyecto

2. Generar app de producción
ng build --prod

3. En la carpeta /dist sacar lo que hay en mapas a /dist y borrar la carpeta mapas

3. Subir a Firebase Hosting
firebase deploy
